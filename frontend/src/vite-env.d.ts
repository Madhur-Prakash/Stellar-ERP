/// <reference types="vite/client" />

/**
 * Vite's ambient types.
 *
 * Supplies `import.meta.env` and the module declarations that make
 * `import '@/styles/globals.css'` type-check. Without this reference, TypeScript
 * treats both as errors even though Vite handles them at build time.
 */
